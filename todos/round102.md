# Round 102 todos（保留给下一轮的 pending 候选）

**已完成 commit**：

- `eec27b0` 上轮记账（round 101）
- `eec27b0` psychology ch12 L895 空 `## ` 标题补全为「九、积极心理学在中国的本土化」+ manifest 同步（本轮主 commit，round 102，drift 7→6）
- （本轮记账 commit 占位，下一轮生成）

**下轮候选**（继承 98/100/101 轮 + 本轮新发现，优先级降序）：

1. **(继承 98/100 轮, 优先级中)** badminton ch12 markdown 与 manifest 严重漂移（11 vs 7）。manifest 后段截断 + h2#2/h2#3 编号错位 + 原版/新版并行保留。结构性问题，需先 grep diff 表再决定动 md 还是动 mn。
2. **(继承 98 轮, 优先级中)** yin-yang 5 章 manifest 与 markdown 漂移（ch08 h2 7→6、ch11/12/13 12→6 截断、ch15 15→7）。需逐章审视后段截断是「全文小结提前」还是「manifest 镜像遗漏」。
3. **(本轮新发现, 优先级低)** psychology ch12 仍有遗留结构怪相：八 之后 L525 标「十一、应用与日常练习」(11.1~11.7)，然后 L865 本章小结，L895 现在补全为「九、中国本土化」(9.1~9.4)，L950 「十、争议与未来」(10.1~10.2)，L986 「十一、日常实践指南」(11.1~11.4)。最终呈现是 十一、九、十、十一 的乱序编号 + 重复「十一」两次。**本轮只补了空 h2，未触动编号顺序**（避免一次 commit 改动面过大）。可远期整理。
5. **(继承 91/95 轮, 优先级低)** 营养书 ch01~ch07 各 400~1000 字偏短，可远期挑 1 章扩写。
6. **(继承 91/95 轮, 优先级低)** `_append_todo_round78.{py,md}` 在 HEAD 里缺失，可远期补一份。
7. **(继承 91/95 轮, 优先级低)** `.gitattributes` 把 .json/.js 当 binary，可远期改成只屏蔽真正需要 lfs 的扩展名。
8. **(本轮新发现, 优先级低)** manifest_data.js 第 4716 行 `{` 是 24 空格（其他同级用 12 空格），孤例 over-indent。本轮已留意但未触碰（避免无关清理）。可远期一次性整理所有 manifest_data.js 缩进怪异点。

**本轮 commit hash**：`eec27b0`

**本轮 push**：✅ 第 4 次重试成功（`e809abf..eec27b0` book → book；累计 sleep 5+10+15+30+60+120 ≈ 240 秒；GitHub Pages 自动部署中）

**本轮 drift 状态**：7→6（psychology/ch12 修复）；剩余 6 处为 yin-yang 5 章 + badminton/ch12（均继承 98 轮以来的待办）

**记账 push**：（下一轮生成）