# 第 81 轮（commit 4731c27）— NSCA-CPT ch02 第十一节后跳号修复

## 本轮做了什么

实地 grep `books/nsca-cpt/ch02-exercise-physiology.md` L1020 → L1096 → L1177 → L1284
发现二级标题序列 `## 十一、常见生理学误区辨析` → `## 十三、运动损伤的生理学` →
`## 十四、营养时机的生理学` → `## 十五、本章总结与下章预告`，**缺少「## 十二」号位**。
- 子节同步跳号：L1098 `### 13.1 急性损伤的生理反应` → L1119 `### 13.2 慢性劳损的生理机制`
  → L1138 `### 13.3 损伤预防的生理学基础` → L1152 `### 13.4 退役/伤停期的生理学管理`，
  全段实际是「十二号位」内容，编号却用 13.1-13.4。
- 本轮最小修复：
  - L1096 `## 十三、运动损伤的生理学——理解身体的"求救信号"` → `## 十二、...`
  - L1098 `### 13.1 急性损伤的生理反应` → `### 12.1 ...`
  - L1119 `### 13.2 慢性劳损的生理机制` → `### 12.2 ...`
  - L1138 `### 13.3 损伤预防的生理学基础` → `### 12.3 ...`
  - L1152 `### 13.4 退役/伤停期的生理学管理` → `### 12.4 ...`
- 章末下游 `## 十四` / `## 十五` 保留原编号（grep 文中「第N节 / 第十N」硬引用 = 0 命中，
  顺移/补节留作下轮候选）
- 零业务代码改动 / 零 ex-lib id 改动（ch02 文件 0 处 [ex:XXXX] 引用）/
  audit 0 drift 不变 / manifest 不动 / APP_VERSION v3.22.62 不 bump
  （沿用 v3.22.55/56/57/62/71/72/73/74/75/78/79/80 等小 fix 不 bump 惯例）

**校验**：
- `node --check app.js` → OK
- `node --check manifest_data.js` → OK
- `python -m json.tool manifest.json` → OK
- `python _audit_exlib_ledger.py` → 0 drift（与改前一致，仅 ch12
  informational list-only）
- `git diff --numstat` → 1 file changed, 5 insertions(+) / 5 deletions(-)
  （每处 1 字 / 1 字替换，5 个标题字改字，零章节内容增删）
- 行尾保护：ch02 文件 LF 行尾原状（`file` 报 Unicode text UTF-8，
  头字节 0x23 无 CRLF）
- byte 变化：56577 → 56577（每处 1 字符 ↔ 1 字符替换，字宽相等）
- 可独立回滚：`git revert HEAD` 即可恢复原 `## 十三 / 13.1-13.4`
- 编号连续性：原 `十一 → 十三 → 十四 → 十五`（跳 12）→ 本轮后
  `十一 → 十二 → 十四 → 十五`（12 占位原 13，13 号位空缺明显，
  下一轮可决定「补节」或「顺移 14→13 / 15→14」）

**用户偏好兑现**：
- 双层结构（前半普通人能看懂 / 后半专业人士可参考）保持 —— 本轮
  是章节号位修正，对两类读者都无阅读影响；专业人士看到 `## 十二`
  与下方子节 `12.1-12.4` 一致，能立刻判断编号连贯
- 沿用 73/74/75/76/77/79/80 轮风格：单次 commit 内含单点修复 +
  双 .py + .md 记账文件追加
- 零伪造 id：ch02 L1096-L1284 区段无 [ex:XXXX] 引用，零 id 改动
- 不做与现有功能重复的大改动：5 字字改字，单字段可独立 git revert

**commit hash**：4731c27

**本轮 push 状态**：
- `git push origin book` 反复重试 10 次均报 `Failed to connect to github.com
  port 443 via 127.0.0.1`（host 网络限制，与 76 轮 ded6cd7 commit message
  提到的 "GitHub Pages push 重试" 同源）。commit 4731c27 已落在本地仓库
  `book` 分支，origin/book 暂时落后于本地（仍是 fdf22c7）。下一轮 push
  重试命令：`git push origin book`；若 443 持续失败可改为 background
  retry + 后台轮询
- 部署延迟风险：s66899.github.io/lamb 站点当前仍是 v3.22.62 (fdf22c7)
  版本；ch02 跳号修复用户尚不可见。这是本轮唯一未尽事项

**下轮候选**：
1. (本轮新增, 优先级中) NSCA-CPT ch02 跳号收尾 —— 本轮把原 13 号节
   改回 12 号位，但「13 号位」现在留空。下一轮可二选一：
   (a) 顺移下游：`## 十四` → `## 十三`、`## 十五` → `## 十四`，章节
   总数从 15 → 14，但所有子节 `14.1-14.5` 需同步改 `13.1-13.5`，
   涉及多个标题字改字但内容不变（5+5=10 字改字，与本轮同型）
   (b) 补节：在「## 十二、运动损伤的生理学」与「## 十四、营养时机」
   之间插入「## 十三、运动表现监测的生理学」(200-400 字新内容)，
   但属内容新增风险较高
2. (本轮新增, 优先级低) NSCA-CPT ch06-agility.md 末尾 L310-L400 是
   第二次结尾（`### 方向变化类型` / `### 敏捷素质组成` /
   `## 敏捷训练方法` / `## 训练计划` / 第二个 `*来源：NSCA敏捷性训练指南*`），
   与上方 L289「## 七、本章总结」+ 思考题 + 参考文献完整结尾重复。
   推测是 2026-07-18 v3.8.5 占位草稿未清理的死章节。下一轮可删
   L309 (含 `*来源：NSCA-CSPS 敏捷性训练指南 + 项目 ex-lib.json*`) 之后
   所有行（删 90 行死内容），与 manifest 注册的 ch06 单章节元数据一致
3. (本轮新增, 优先级低) NSCA-CPT ch03-anatomy.md 193 行只有 6 个
   无编号二级标题（`## 羽毛球运动解剖基础` / `## 关键肌肉功能` /
   `## 关节活动度` / `## 动力链分析` / `## 常见损伤与解剖` /
   `## 训练建议`），与 NSCA 其他章「一、二、三」中文章节号风格脱节。
   下一轮可加 6 字编号（最小 = 6 个标题字加字）
4. (继承 71~80 轮, 优先级低) 营养书 ch01~ch07 各 400~1000 字偏短
   （实为完整骨架 + 公式 + 表），如需扩写可挑 1 章做小补
5. (继承 71~80 轮, 优先级低) 羽毛球康复书 ch07-achilles 184 行最薄，
   但 inline 32 处 / unique 14 个已饱和，结构完整，硬补有 scope
   creep 风险，留观
6. (继承 71~80 轮, 优先级低) NSCA-CPT ch10 §七末段四次勘误 blockquote
   累积 580+ 字，可远期整理到附录「v3.22 勘误史」独立 H2
7. (继承 80 轮, 优先级低) `ch10-recovery.md` §3.2 / §3.3 / §四 / §五
   行文里仍有几处把 12 条 SMR 笼统称"泡沫轴"或"SMR"——可分轮挑一处
   把"同时包含 ex-5210 / ex-5211 的段落"补 1 句"其中 ex-5210/5211
   为筋膜球专项"小注
8. (继承 80 轮, 优先级低) `_append_todo_round78.{py,md}` 在 HEAD 里
   缺失 —— 78 轮的记账 narrative 写在 `_session_todo.md` 末尾「## 第
   78 轮」段，但未生成 73~79/80 双写惯例的两个文件。可远期补一份
   让 round68/71/73/74/75/76/77/79/80/81 双写系列保持连续