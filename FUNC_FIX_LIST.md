# 🏸 Lamb 羽毛球训练系统 · 功能整改清单

> **项目**：s66899.github.io/lamb  
> **版本**：v3.8.6  
> **日期**：2026-07-30

---

## 🚨 P0 - 立即修复（阻塞性问题）

| # | 问题 | 位置 | 根因 | 修复方案 |
|---|------|------|------|----------|
| 1 | **白天模式对比度失效** | `style.css` / `app.js` 主题逻辑 | CSS 变量不匹配 `:root`，浅色态下仍是深色配色 | 新增 `[data-theme="light"]` 显式覆盖 |
| 2 | **"开始训练"按钮失效** | `app.js` 第 3435 行 | `TRAIN_MODULES` 缺 `topics` 映射，`openModuleTopic` 降级逻辑错误 | 补充 `topics` 字段或修复降级逻辑 |

---

## 🔥 P1 - 高优（影响留存）

| # | 问题 | 位置 | 现状 | 影响 |
|---|------|------|------|------|
| 3 | **搜索功能空壳** | `app.js` `openSearch()` | 只有 UI，无实际搜索逻辑 | 用户无法快速定位内容 |
| 4 | **进度数据全 0** | `app.js` 学员面板 | `getP()` 读取逻辑与存储格式不匹配 | 校准 localStorage 读写逻辑 |
| 5 | **可访问性缺失** | 全局 | 全部用 `<div>` 做按钮，无 `tabindex`/`aria-label`/键盘回车 | 视障用户完全无法使用 |
| 6 | **学员问卷缺失** | `app.js` | 角色选择后未触发水平评估问卷 | 新学员无法建立基线数据 |

---

## 🛠 P2 - 中优（体验完善）

| # | 问题 | 位置 | 现状 | 影响 |
|---|------|------|------|------|
| 7 | **阅读器目录移动端失效** | `app.js` / `style.css` | 480px 以下 `display:none` 强制隐藏 | 用户无法在手机上导航章节 |
| 8 | **侧边栏移动端遮罩缺失** | `style.css` | 480px 以下无遮罩层，点击会穿透 | 体验割裂 |
| 9 | **无滑动手势** | `app.js` | 阅读器无 swipe 返回 | 移动端操作效率低 |
| 10 | **字体大小无边界** | `app.js` `increaseFont()` | 边界值未生效 | 特定用户群体不适配 |
| 11 | **数据无法同步** | 全局 | 进度只在 localStorage，多设备无法同步 | 换设备 = 进度归零 |

---

## 📚 P3 - 长期增强

| # | 问题 | 位置 | 现状 | 影响 |
|---|------|------|------|------|
| 12 | **PWA 缺失** | `index.html` | 无 manifest/install prompt/离线 | 无法添加到桌面 |
| 13 | **SEO 基本为 0** | `index.html` | 无 meta description/og/twitter:card | 社交分享丑 |
| 14 | **i18n 框架空缺** | 全局 | 硬编码中文 | 无法扩展其他语言 |
| 15 | **无错误上报入口** | `app.js` | 只能看 console | 线上问题难追踪 |
| 16 | **代码架构 monolith** | `app.js` | 290KB 单文件，无模块化 | 维护困难 |

---

## 📋 整改执行顺序

```
第1轮（P0，2小时）
├── 1.1 修复白天模式对比度
└── 1.2 修复"开始训练"按钮

第2轮（P1.1-P1.3，1天）
├── 2.1 搜索功能落地
├── 2.2 进度数据校准
└── 2.3 可访问性基础修复

第3轮（P1.4-P1.6，1天）
├── 3.1 学员问卷补全
├── 3.2 移动端目录/遮罩
└── 3.3 滑动手势

第4轮（P2，2天）
├── 4.1 数据导出/导入（多设备同步前置）
├── 4.2 PWA 基础
└── 4.3 错误处理优化

第5轮（P3，长期）
├── 5.1 SEO Meta
├── 5.2 i18n 框架
└── 5.3 代码拆包重构
```

---

## ✅ 验收标准

| 阶段 | 验收条件 |
|------|----------|
| P0 | 白天/夜间模式切换正常；"开始训练"能跳转到具体章节 |
| P1 | 搜索能返回结果；进度条正常显示；Tab 键可遍历所有按钮 |
| P2 | 移动端目录可用；侧边栏有遮罩；滑动返回生效 |
| P3 | 可添加到桌面；社交分享有预览图；支持中英双语 |

---

## 📎 相关文件

- `index.html` - 主页面
- `app.js` - 核心逻辑 (3867 行)
- `style.css` - 样式
- `manifest_data.js` - 内容清单
- `product_optimization_checklist.md` - 原始诊断记录
